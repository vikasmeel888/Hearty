<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bmop">

    <application
        android:name=".GoogleLoginApplication"
        android:allowBackup="true"
        android:icon="@mipmap/logoo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logoo"
        android:supportsRtl="true"
        android:theme="@style/Theme.BMOP">
        <activity android:name=".activities.MessagesActivity"></activity>
        <activity android:name=".activities.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.HomeActivity" />
        <activity android:name=".activities.RegistrationActivity" />
        <activity android:name=".activities.BaseActivity" />
        <activity android:name=".activities.MedicineActivity" />
        <activity android:name=".activities.BloodActivity" />
        <activity android:name=".activities.DonateBloodActivity" />
        <activity android:name=".activities.EquipmentActivity" />
        <activity android:name=".activities.SellEquipmentActivity" />
        <activity android:name=".activities.SellMedicineActivity" />
        <activity android:name=".activities.ChatActivity" />
        <activity android:name=".activities.RemoveActivity" />
    </application>

</manifest>